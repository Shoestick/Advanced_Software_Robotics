from setuptools import find_packages
from setuptools import setup

setup(
    name='assign1_interfaces',
    version='0.0.0',
    packages=find_packages(
        include=('assign1_interfaces', 'assign1_interfaces.*')),
)
